search:
  categories:
    - "cs.AI"
  keywords:
    - "LLM"
  max_results: 10

summary:
  prompt_template: |
    以下の論文を日本語で簡潔に要約してください。

    タイトル: {title}

    概要:
    {abstract}

    要約は以下の形式で出力してください：
    - 研究の目的（1-2文）
    - 手法の概要（1-2文）
    - 主な結果・貢献（1-2文）

notification:
  slack_enabled: false
  discord_enabled: true
